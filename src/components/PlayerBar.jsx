const PlayerBar = () => {
  return (
    // 'h-24' (hauteur fixe) et 'bg-zinc-800' (fond sombre)
    <footer className="bg-zinc-800 text-white p-4 h-24 border-t border-black">
      <p>Barre de Lecture (Player Bar)</p>
    </footer>
  );
};

export default PlayerBar;